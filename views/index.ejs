<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
    	<% for (var i = 0; i < articles.length; i++) { %>
    		<li>
    		<a href="/article/<%= articles[i].title %>"><%= articles[i].title %></a>
          <small><%= articles[i].date.toLocaleDateString() %></small>
    		</li>
    	<% };%>
    </ul>
  </body>
</html>
